var cancellable = function(fn, args, t) {
     const timerId = setTimeout(() => {
        fn(...args);
    }, t);
    
    const cancelFn = () => {
        clearTimeout(timerId);
    };
    
    return cancelFn;
};
